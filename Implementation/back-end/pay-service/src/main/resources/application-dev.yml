spring:
  datasource:
    url: jdbc:mysql://localhost:3306/icedamericano_mall?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
  data:
    redis:
      host: localhost
      port: 6379

# ==================== 微信支付（开发环境使用 Mock 桩代码） ====================
# 设置 merchant-id 为有效值即可激活真实微信支付
wechat:
  pay:
    merchant-id:  # 留空 = 自动使用 MockWechatPaymentClient
    private-key-path: ""
    merchant-serial-number: ""
    api-v3-key: ""
    notify-url: ""

logging:
  level:
    org.icedamericanomall: DEBUG
